server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/aiqueen}
    username: ${JDBC_DATABASE_USERNAME:postgres}
    password: ${JDBC_DATABASE_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

aiq:
  openai-key: ${AIQ_OPENAI_KEY:}
  node-url: ${AIQ_NODE_URL:}
  donation-static-url: ${AIQ_DONATION_URL:}
stripe:
  secret-key: ${STRIPE_SECRET_KEY:}
twilio:
  account-sid: ${TWILIO_ACCOUNT_SID:}
  auth-token: ${TWILIO_AUTH_TOKEN:}
  whatsapp-number: ${TWILIO_WHATSAPP_NUMBER:}
